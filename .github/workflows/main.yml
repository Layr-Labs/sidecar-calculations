name: build
on: push
jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
        with:
          platforms: arm64

      - name: Set up ARM64 chroot environment
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-user-static debootstrap
          sudo qemu-debootstrap --arch=arm64 --variant=minbase focal /arm64-chroot
          sudo cp /usr/bin/qemu-aarch64-static /arm64-chroot/usr/bin/

      - name: Install build dependencies in chroot
        run: |
          sudo chroot /arm64-chroot apt-get update
          sudo chroot /arm64-chroot apt-get install -y build-essential
          sudo mkdir chroot /arm64-chroot/build
          sudo cp -r . /arm64-chroot/build
          sudo chroot /arm64-chroot /bin/bash -c "pwd"
